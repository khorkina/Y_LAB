package Ylab;

import java.time.Period;

public class StatisticsService {
    public int calculateStreak(Habit habit) {
        // Логика расчета серии выполнения
        return 0;
    }

    public double calculateSuccessRate(User user, Habit habit, Period period) {
        // Логика расчета процента выполнения
        return 0;
    }
}
